package io.commoncore.audit;

/**
 * Enumeration of audit actions
 */
public enum AuditAction {
    CREATE,
    UPDATE,
    DELETE,
    READ,
    SEARCH,
    LOGIN,
    LOGOUT,
    EXPORT,
    IMPORT,
    CUSTOM
}
